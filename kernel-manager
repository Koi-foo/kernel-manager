#!/bin/sh
# Запуск и на будущее для передачи необходимых атрибутов для pak и im.
# На текущий момент создание обертки pkexec в скрипте kernel_manager.py нецелесообразно.
# Однако если потребуется резкое увеличение безопасности, то создание обертки и поставка
# программы в компилируемом виде предотвратит любое изменение кода пользователем.
pkexec "/opt/kernel-manager/kernel_manager.py" "$@"
